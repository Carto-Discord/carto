{
  "name": "carto-client",
  "version": "0.1.0",
  "scripts": {
    "prebuild": "rimraf build/",
    "postbuild": "copyfiles src/**/*.html build/",
    "build": "tsc",
    "build:package": "copyfiles build/**/* package.json package-lock.json package",
    "register": "node ./build/src/registerCommands.ts",
    "start": "functions-framework --source=build/src/ --target=slashFunction",
    "test": "jest --watch",
    "test:ci": "jest",
    "watch": "concurrently \"tsc -w\" \"nodemon --watch ./build/ --exec npm run start\""
  },
  "author": "Tohaker",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^1.7.1",
    "@google-cloud/storage": "^5.8.3",
    "discord.js": "^12.5.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "google-auth-library": "^7.0.4",
    "slash-commands": "^1.4.0",
    "tweetnacl": "^1.0.3",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "concurrently": "^6.0.2",
    "copyfiles": "^2.4.1",
    "jest": "^26.6.3",
    "jest-express": "^1.12.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.4"
  }
}
